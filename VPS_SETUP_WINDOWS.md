# 🖥️ Запуск бота на Windows VPS

## 📋 Что нужно сделать

### 1. Подключитесь к VPS

Используйте **Remote Desktop Connection** (RDP):
1. Нажмите `Win + R`
2. Введите `mstsc`
3. Введите IP адрес вашего VPS
4. Введите логин и пароль

### 2. Установите Python

#### Вариант А: Через официальный сайт
1. Скачайте Python 3.11+ с [python.org](https://www.python.org/downloads/)
2. При установке **ОБЯЗАТЕЛЬНО** отметьте:
   - ✅ "Add Python to PATH"
   - ✅ "Install pip"

#### Вариант Б: Через winget (Windows 11)
```powershell
winget install Python.Python.3.11
```

### 3. Установите зависимости бота

Откройте **PowerShell** или **Command Prompt** и выполните:

```powershell
# Перейдите в папку с ботом
cd C:\path\to\your\project\scripts

# Установите зависимости
pip install aiogram pyrogram aiohttp
```

Или создайте файл `requirements.txt` в папке `scripts/`:

```txt
aiogram>=3.0.0
pyrogram>=2.0.0
aiohttp>=3.9.0
```

И установите:
```powershell
pip install -r requirements.txt
```

### 4. Настройте settings.json

Откройте `scripts/settings.json` и укажите URL вашего Vercel приложения:

```json
{
  "bot_token": "ваш_токен_бота",
  "log_bot_token": "ваш_токен_логов",
  "api_id": 39831972,
  "api_hash": "ваш_api_hash",
  "api_url": "https://your-app.vercel.app",
  "admin_ids": [ваш_admin_id],
  "workers": [ваш_worker_id],
  "target_user": "@ваш_username",
  "banker_session": "номер_телефона"
}
```

### 5. Запустите бота

#### Вариант А: Через PowerShell/CMD
```powershell
cd C:\path\to\your\project\scripts
python unified_bot_v3.py
```

#### Вариант Б: Создайте .bat файл для запуска

Создайте файл `start_bot.bat` в папке `scripts/`:

```batch
@echo off
cd /d "%~dp0"
python unified_bot_v3.py
pause
```

Дважды кликните на `start_bot.bat` для запуска.

### 6. Запуск бота в фоне (важно!)

Чтобы бот работал даже когда вы закрыли RDP:

#### Вариант А: Через Task Scheduler (рекомендуется)

1. Нажмите `Win + R`, введите `taskschd.msc`
2. Создайте новую задачу:
   - **Имя**: "Telegram Bot"
   - **Триггер**: "При входе в систему" или "При запуске компьютера"
   - **Действие**: "Запустить программу"
     - Программа: `C:\Python311\python.exe` (или ваш путь к Python)
     - Аргументы: `C:\path\to\your\project\scripts\unified_bot_v3.py`
     - Рабочая папка: `C:\path\to\your\project\scripts`
   - **Условия**: 
     - ✅ Запускать задачу независимо от того, выполнен ли вход пользователя в систему
   - **Параметры**:
     - ✅ Выполнять задачу при отсутствии пользователя

#### Вариант Б: Через NSSM (Non-Sucking Service Manager)

1. Скачайте [NSSM](https://nssm.cc/download)
2. Распакуйте в папку (например `C:\nssm`)
3. Откройте PowerShell от имени администратора:
```powershell
cd C:\nssm\win64
.\nssm install TelegramBot
```

В открывшемся окне укажите:
- **Path**: `C:\Python311\python.exe` (путь к Python)
- **Startup directory**: `C:\path\to\your\project\scripts`
- **Arguments**: `unified_bot_v3.py`

Затем:
```powershell
nssm start TelegramBot
```

#### Вариант В: Через Screen/Tmux (если установлен WSL)

Если у вас установлен WSL (Windows Subsystem for Linux):
```bash
wsl
cd /mnt/c/path/to/your/project/scripts
screen -S bot
python unified_bot_v3.py
# Нажмите Ctrl+A, затем D для отсоединения
```

## 🔧 Проверка работы

### 1. Проверьте логи

Бот должен выводить:
```
[INFO] BOT v3.0 STARTED
[INFO] Polling API...
[INFO] Found X pending request(s)
```

### 2. Проверьте подключение к Vercel

В логах не должно быть ошибок типа:
- `Connection refused`
- `Timeout`
- `404 Not Found`

### 3. Тестовая авторизация

1. Откройте ваше приложение на Vercel
2. Попробуйте авторизоваться
3. Проверьте логи бота - должны появиться запросы

## 🛠 Решение проблем

### Проблема: Python не найден

**Решение:**
```powershell
# Проверьте установку
python --version

# Если не работает, добавьте Python в PATH вручную:
# Система → Дополнительные параметры системы → Переменные среды
# Добавьте путь к Python в PATH
```

### Проблема: Модули не найдены

**Решение:**
```powershell
pip install --upgrade pip
pip install aiogram pyrogram aiohttp
```

### Проблема: Бот останавливается при закрытии RDP

**Решение:**
Используйте Task Scheduler или NSSM (см. выше)

### Проблема: Ошибка подключения к API

**Решение:**
1. Проверьте `api_url` в `settings.json`
2. Убедитесь, что URL доступен: откройте в браузере на VPS
3. Проверьте firewall - разрешите исходящие соединения

### Проблема: Сессии не сохраняются

**Решение:**
1. Убедитесь, что папка `scripts/sessions/` существует
2. Проверьте права доступа к папке
3. Запустите бота от имени администратора (если нужно)

## 📝 Автозапуск при перезагрузке

### Через Task Scheduler:

1. Откройте Task Scheduler
2. Найдите вашу задачу "Telegram Bot"
3. Правый клик → Properties
4. Вкладка "Триггеры" → Добавить
5. Выберите "При запуске компьютера"
6. ✅ Включено

## 🔒 Безопасность

1. **Не храните токены в коде** - используйте `settings.json` или переменные окружения
2. **Ограничьте доступ к VPS** - используйте сильные пароли
3. **Настройте firewall** - разрешите только нужные порты
4. **Регулярно обновляйте** Python и зависимости

## 📊 Мониторинг

### Проверка работы бота:

```powershell
# Проверьте процессы Python
Get-Process python

# Проверьте логи (если настроено логирование в файл)
Get-Content C:\path\to\bot.log -Tail 50
```

### Перезапуск бота:

```powershell
# Остановите процесс
Stop-Process -Name python

# Запустите снова
cd C:\path\to\your\project\scripts
python unified_bot_v3.py
```

## ✅ Чеклист настройки

- [ ] Python установлен и добавлен в PATH
- [ ] Зависимости установлены (`pip install ...`)
- [ ] `settings.json` настроен с правильным `api_url`
- [ ] Бот запускается и работает
- [ ] Бот подключен к Vercel (нет ошибок в логах)
- [ ] Настроен автозапуск (Task Scheduler или NSSM)
- [ ] Бот работает после перезагрузки VPS
- [ ] Бот работает при закрытом RDP

## 🚀 Готово!

Теперь ваш бот работает на VPS и подключен к Vercel!

---

💡 **Совет**: Для удобства создайте ярлык на рабочем столе для быстрого запуска бота.

