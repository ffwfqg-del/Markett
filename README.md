# Telegram Marketplace Bot

Next.js приложение для маркетплейса Telegram подарков с интеграцией бота.

## 🚀 Деплой на Vercel

### Шаг 1: Подготовка репозитория

1. Создайте репозиторий на GitHub
2. Закоммитьте и запушьте код:
```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/your-username/your-repo.git
git push -u origin main
```

### Шаг 2: Деплой на Vercel

1. Зайдите на [vercel.com](https://vercel.com)
2. Войдите через GitHub
3. Нажмите "Add New Project"
4. Выберите ваш репозиторий
5. Vercel автоматически определит Next.js проект

### Шаг 3: Настройка переменных окружения

В настройках проекта Vercel добавьте следующие переменные окружения:

#### Обязательные:
```
NEXT_PUBLIC_BOT_TOKEN=your_bot_token
NEXT_PUBLIC_BOT_USERNAME=your_bot_username
NEXT_PUBLIC_API_URL=https://your-app.vercel.app
API_ID=your_api_id
API_HASH=your_api_hash
ADMIN_IDS=your_admin_id
```

#### Опциональные:
```
LOG_BOT_TOKEN=your_log_bot_token (для логирования)
DATABASE_URL=your_database_url (если используете внешнюю БД)
```

### Шаг 4: Настройка проекта

1. **Framework Preset**: Next.js (определится автоматически)
2. **Root Directory**: `./` (корень проекта)
3. **Build Command**: `npm run build` (по умолчанию)
4. **Output Directory**: `.next` (по умолчанию)
5. **Install Command**: `npm install` (по умолчанию)

### Шаг 5: Деплой

1. Нажмите "Deploy"
2. Дождитесь завершения сборки
3. После успешного деплоя получите URL вида: `https://your-app.vercel.app`

### Шаг 6: Обновление настроек

После деплоя обновите `NEXT_PUBLIC_API_URL` в переменных окружения на ваш Vercel URL.

## 📝 Важные замечания

1. **Python бот** (`scripts/`) не деплоится на Vercel - он должен работать отдельно на сервере
2. **Сессии бота** не должны попадать в репозиторий (добавлены в .gitignore)
3. **Переменные окружения** с секретами не должны быть в коде
4. **База данных** - если используете SQLite, она не будет работать на Vercel (нужна внешняя БД)

## 🔧 Локальная разработка

```bash
npm install
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000)

## 🔗 Подключение бота к Vercel

После деплоя на Vercel нужно подключить Python бота:

1. **Получите URL** вашего Vercel приложения (например: `https://your-app.vercel.app`)
2. **Обновите** `scripts/settings.json`:
   ```json
   {
     "api_url": "https://your-app.vercel.app"
   }
   ```
3. **Запустите бота**:
   ```bash
   cd scripts
   python unified_bot_v3.py
   ```

📖 **Подробные инструкции:**
- ⚡ Быстрая настройка: [`BOT_SETUP_VERCEL.md`](./BOT_SETUP_VERCEL.md)
- 📚 Полная инструкция: [`BOT_VERCEL_CONNECTION.md`](./BOT_VERCEL_CONNECTION.md)
- 📊 Сводка: [`CONNECTION_SUMMARY.md`](./CONNECTION_SUMMARY.md)

## 📦 Структура проекта

```
├── app/              # Next.js App Router
│   └── api/          # API endpoints для бота
├── components/       # React компоненты
├── lib/              # Утилиты и библиотеки
├── contexts/         # React контексты
├── public/           # Статические файлы
└── scripts/          # Python бот (не деплоится, работает отдельно)
```

## 🛠 Технологии

- Next.js 14
- React 18
- TypeScript
- Tailwind CSS
- Telegram Web App API

