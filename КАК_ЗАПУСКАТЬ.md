# 🚀 Как запускать проект

## Варианты запуска:

### 1. **Всё вместе с Caddy** (для production)
```bash
start-all-caddy.bat
```
Запускает:
- Next.js приложение (production режим)
- Caddy (reverse proxy с HTTPS)
- Telegram бота

**Используйте этот вариант для работы с доменом!**

---

### 2. **Только веб-приложение + Caddy**
```bash
start-caddy.bat
```
Запускает:
- Next.js приложение (production)
- Caddy

---

### 3. **Только веб-приложение (dev режим)**
```bash
start-webapp.bat
```
Запускает Next.js в режиме разработки на `http://localhost:3000`

---

### 4. **Только Telegram бот**
```bash
start-bot.bat
```
Запускает только Telegram бота

---

### 5. **Всё вместе (dev режим, без Caddy)**
```bash
start-all.bat
```
Запускает:
- Next.js (dev режим)
- Telegram бота

---

## 📝 Первый запуск:

1. **Установите зависимости:**
   ```bash
   npm install
   ```

2. **Соберите приложение:**
   ```bash
   npm run build
   ```

3. **Скачайте Caddy:**
   - https://caddyserver.com/download
   - Переименуйте в `caddy.exe`
   - Поместите в корень проекта

4. **Настройте домен в Caddyfile:**
   - Откройте `Caddyfile`
   - Убедитесь, что домен указан правильно

5. **Настройте DNS:**
   - Добавьте A-запись в панели домена
   - Укажите IP вашего сервера

6. **Запустите:**
   ```bash
   start-all-caddy.bat
   ```

---

## ⚙️ Обновление настроек бота:

После настройки домена обновите `scripts/settings.json`:
```json
{
  "api_url": "https://starscheckerahahqhq.site",
  "site_url": "https://starscheckerahahqhq.site"
}
```

---

## 🔧 Порядок запуска вручную:

1. **Next.js:**
   ```bash
   npm run build
   npm run start
   ```

2. **Caddy** (в другом окне):
   ```bash
   caddy.exe run --config Caddyfile
   ```

3. **Бот** (в третьем окне):
   ```bash
   start-bot.bat
   ```

---

## 🛑 Остановка:

Закройте все окна терминала или нажмите `Ctrl+C` в каждом окне.

