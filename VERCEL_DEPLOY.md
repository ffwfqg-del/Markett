# 🚀 Инструкция по деплою на Vercel

## Быстрый старт

### 1. Подготовка GitHub репозитория

```bash
# Инициализация git (если еще не сделано)
git init

# Добавление всех файлов
git add .

# Коммит
git commit -m "Ready for Vercel deployment"

# Добавление remote (замените на ваш репозиторий)
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git

# Push
git push -u origin main
```

### 2. Деплой на Vercel

1. Перейдите на [vercel.com](https://vercel.com)
2. Войдите через **GitHub**
3. Нажмите **"Add New Project"**
4. Выберите ваш репозиторий
5. Vercel автоматически определит Next.js

### 3. Настройка переменных окружения

В разделе **Settings → Environment Variables** добавьте:

#### 🔑 Обязательные переменные:

| Переменная | Описание | Пример |
|-----------|----------|--------|
| `NEXT_PUBLIC_BOT_TOKEN` | Токен Telegram бота | `8503312636:AAFn83fVJW...` |
| `NEXT_PUBLIC_BOT_USERNAME` | Username бота (без @) | `your_bot_username` |
| `NEXT_PUBLIC_API_URL` | URL вашего приложения | `https://your-app.vercel.app` |
| `API_ID` | Telegram API ID | `39831972` |
| `API_HASH` | Telegram API Hash | `037087fc71eab9ce52397d7001c31520` |
| `ADMIN_IDS` | ID админов (через запятую) | `6233384461` |

#### 📝 Опциональные переменные:

| Переменная | Описание |
|-----------|----------|
| `LOG_BOT_TOKEN` | Токен бота для логирования |
| `DATABASE_URL` | URL базы данных (если используете внешнюю) |

### 4. Важно! 

**После первого деплоя:**

1. Скопируйте URL вашего приложения (например: `https://your-app.vercel.app`)
2. Вернитесь в **Settings → Environment Variables**
3. Обновите `NEXT_PUBLIC_API_URL` на ваш Vercel URL
4. Нажмите **Redeploy** для применения изменений

### 5. Настройки проекта в Vercel

- **Framework Preset**: Next.js (автоматически)
- **Root Directory**: `./`
- **Build Command**: `npm run build`
- **Output Directory**: `.next`
- **Install Command**: `npm install`

### 6. Проверка деплоя

После деплоя проверьте:
- ✅ Приложение открывается по URL
- ✅ API endpoints работают (`/api/...`)
- ✅ Telegram Web App работает
- ✅ Нет ошибок в логах Vercel

## 🔧 Что НЕ деплоится на Vercel

Следующие файлы/папки исключены (через `.vercelignore`):
- `scripts/` - Python бот (должен работать отдельно)
- `*.bat`, `*.sh` - скрипты для локального запуска
- `*.db`, `*.session` - базы данных и сессии
- `docs/` - документация

## ⚠️ Важные замечания

1. **Python бот** должен работать на отдельном сервере (не на Vercel)
2. **База данных SQLite** не работает на Vercel - нужна внешняя БД (PostgreSQL, MongoDB и т.д.)
3. **Переменные окружения** с секретами храните только в Vercel, не в коде
4. **Сессии бота** не должны попадать в репозиторий

## 🐛 Решение проблем

### Ошибка сборки
- Проверьте логи в Vercel Dashboard
- Убедитесь, что все зависимости в `package.json`
- Проверьте TypeScript ошибки

### API не работает
- Проверьте `NEXT_PUBLIC_API_URL` в переменных окружения
- Убедитесь, что URL правильный (с https://)

### Telegram Web App не работает
- Проверьте `NEXT_PUBLIC_BOT_TOKEN` и `NEXT_PUBLIC_BOT_USERNAME`
- Убедитесь, что бот настроен правильно в BotFather

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи в Vercel Dashboard
2. Проверьте переменные окружения
3. Убедитесь, что все зависимости установлены

