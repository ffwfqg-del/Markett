# Настройка хостинга через Caddy

Это руководство поможет вам настроить хостинг вашего Next.js приложения через Caddy с использованием собственного домена.

## 📋 Требования

1. **Caddy** - скачайте с [caddyserver.com](https://caddyserver.com/download)
   - Для Windows: `caddy_windows_amd64.exe` или `caddy.exe`
2. **Домен** с возможностью настройки DNS записей
3. **Сервер/VPS** с Windows (или Linux) и открытыми портами 80 и 443

## 🚀 Шаги настройки

### 1. Подготовка Next.js приложения

Сначала соберите приложение для production:

```bash
npm run build
```

### 2. Настройка Caddyfile

1. Откройте файл `Caddyfile` в корне проекта
2. Замените `yourdomain.com` на ваш реальный домен:
   ```
   example.com {
       reverse_proxy localhost:3000
   }
   ```

### 3. Настройка DNS записей

В панели управления вашего домена (где вы покупали домен) добавьте DNS записи:

#### Вариант A: Если Caddy на том же сервере, что и домен
```
Тип: A
Имя: @ (или оставьте пустым для основного домена)
Значение: IP-адрес вашего сервера
TTL: 3600 (или автоматически)
```

#### Вариант B: Если используете поддомен
```
Тип: A
Имя: www (или app, или любой другой поддомен)
Значение: IP-адрес вашего сервера
TTL: 3600
```

#### Пример для Cloudflare, Namecheap, GoDaddy и т.д.:
- **Cloudflare**: DNS → Records → Add record → Type: A, Name: @, IPv4: ваш IP
- **Namecheap**: Advanced DNS → Add New Record → Type: A Record, Host: @, Value: ваш IP
- **GoDaddy**: DNS Management → Add → Type: A, Name: @, Value: ваш IP

### 4. Проверка портов

Убедитесь, что порты открыты в firewall:

**Windows Firewall:**
```powershell
# Открыть порт 80 (HTTP)
New-NetFirewallRule -DisplayName "Caddy HTTP" -Direction Inbound -LocalPort 80 -Protocol TCP -Action Allow

# Открыть порт 443 (HTTPS)
New-NetFirewallRule -DisplayName "Caddy HTTPS" -Direction Inbound -LocalPort 443 -Protocol TCP -Action Allow
```

**Или через GUI:**
- Панель управления → Брандмауэр Windows → Дополнительные параметры
- Правила для входящих подключений → Создать правило
- Порт → TCP → 80 и 443

### 5. Запуск приложения

#### Вариант A: Ручной запуск

1. **Запустите Next.js приложение** (в отдельном окне терминала):
   ```bash
   npm run start
   ```
   Приложение будет работать на `localhost:3000`

2. **Запустите Caddy** (в другом окне терминала):
   ```bash
   caddy.exe run
   ```
   Или для Windows Service:
   ```bash
   caddy.exe run --config Caddyfile
   ```

#### Вариант B: Автоматический запуск (скрипт)

Используйте файл `start-caddy.bat` (создан ниже) для автоматического запуска.

### 6. Проверка работы

1. Подождите 1-2 минуты для получения SSL сертификата
2. Откройте браузер и перейдите на `https://yourdomain.com`
3. Должен появиться ваш сайт с зеленым замком (HTTPS)

## 🔧 Дополнительные настройки

### Изменение порта Next.js

Если хотите использовать другой порт (например, 3001):

1. В `Caddyfile` измените:
   ```
   reverse_proxy localhost:3001
   ```

2. Запустите Next.js с указанием порта:
   ```bash
   PORT=3001 npm run start
   ```
   Или создайте `.env` файл:
   ```
   PORT=3001
   ```

### Логирование

Логи Caddy будут сохраняться в папке `logs/` (создайте её, если её нет).

### Перезапуск Caddy

После изменения `Caddyfile`:
```bash
# Остановите Caddy (Ctrl+C)
# Запустите снова
caddy.exe run
```

## 🛠️ Устранение проблем

### Проблема: "Certificate not obtained"

**Решение:**
- Убедитесь, что порты 80 и 443 открыты
- Проверьте, что DNS записи правильно настроены (может занять до 24 часов)
- Проверьте, что домен указывает на правильный IP

### Проблема: "Connection refused"

**Решение:**
- Убедитесь, что Next.js приложение запущено на порту 3000
- Проверьте, что в `Caddyfile` указан правильный порт

### Проблема: "502 Bad Gateway"

**Решение:**
- Убедитесь, что Next.js приложение запущено
- Проверьте логи Caddy: `logs/access.log`
- Проверьте, что приложение слушает на `localhost:3000`, а не `127.0.0.1:3000`

## 📝 Структура файлов

```
Markett/
├── Caddyfile          # Конфигурация Caddy
├── package.json       # Next.js зависимости
├── .next/            # Собранное приложение (после npm run build)
└── logs/             # Логи Caddy (создайте вручную)
```

## 🔒 Безопасность

Caddy автоматически:
- Получает SSL сертификаты через Let's Encrypt
- Обновляет сертификаты автоматически
- Перенаправляет HTTP на HTTPS

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи Caddy
2. Проверьте, что Next.js приложение работает локально
3. Проверьте DNS записи через `nslookup yourdomain.com`

