# ⚡ Быстрая настройка бота для Vercel

## 🎯 Что нужно сделать

### 1. Получите URL Vercel приложения

После деплоя у вас будет URL вида:
```
https://your-app-name.vercel.app
```

### 2. Обновите settings.json

Откройте `scripts/settings.json` и измените `api_url`:

```json
{
  "api_url": "https://your-app-name.vercel.app"
}
```

**Важно:** 
- ✅ Используйте `https://`
- ✅ Без `/` в конце
- ✅ URL должен быть доступен из интернета

### 3. Запустите бота

```bash
cd scripts
python unified_bot_v3.py
```

## ✅ Готово!

Бот автоматически подключится к Vercel и начнет работать.

## 🔍 Проверка

### Проверьте логи бота:
```
[INFO] Polling API...
[INFO] Found X pending request(s)
```

### Проверьте API вручную:
```bash
curl https://your-app-name.vercel.app/api/telegram/get-pending
```

Должен вернуть JSON с pending requests.

## 📝 Альтернатива: переменные окружения

Вместо `settings.json` можно использовать переменные окружения:

```bash
export API_URL="https://your-app-name.vercel.app"
export BOT_TOKEN="ваш_токен"
export API_ID=39831972
export API_HASH="ваш_hash"
python unified_bot_v3.py
```

---

📖 Подробная инструкция: `BOT_VERCEL_CONNECTION.md`

